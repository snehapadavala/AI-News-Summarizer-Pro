# empty set is represented in set()
# not allow duplicate values
# by using add() we can add only one value,but using update() we can add multiple values
# 
# union is rep as (a|b) or (a.union(b))
#  
# intersection as (a&b) 

# difference (a-b)gives elements in a except elements in b
# (b-a) gives elements in b except elements in a
# symmettric diff (a^b) or (a.symmetric_difference(b))


a=40
b=20
print(a/b)
print(a//b) # gives quotient only as integer value ex:3.7 means gives 3.
print(a%b) # modules gives reminder value as output
